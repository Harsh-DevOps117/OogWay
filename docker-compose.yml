version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - app/.env
    volumes:
      - ./app:/app/app
      # Mount the episodes directory if needed by the app
      - ./episodes:/app/episodes
    restart: always
